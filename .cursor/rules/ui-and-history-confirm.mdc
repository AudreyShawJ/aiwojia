---
description: UI 与影响历史/审计的业务逻辑须先与用户确认
alwaysApply: true
---

# UI 与历史业务逻辑

在未得到用户**明确同意**前，不得擅自：

- 修改界面文案、布局、图标、交互流程（含侧滑、按钮标签、Modal 等）。
- 修改会改变**数据含义、可追溯性、审计语义**的业务逻辑（例如：`completed_at` / `completed_by`、完成 vs 取消、软删与硬删、财务流水来源等）。

若任务涉及上述范围，应先列出拟议改动并**等待用户确认**后再改代码；用户仅要求排查 bug 时，优先在**不改 UI**的前提下用日志、测试或后端逻辑修复。
