/**
 * 数字服务订阅语境：用语义关键词匹配，**不枚举**具体平台/App 名。
 * 未出现下列词但明显是「某软件/网站的会员、自动续费」时，由意图分类器（见 chat 分类 prompt）识别为同类。
 */
export const DIGITAL_SUBSCRIPTION_CONTEXT_RE =
  /订阅|续订|会员费|自动续费|连续包月|连续包年|免密支付|免密扣款/i;

/** 助手刚追问过「按月 / 按年」类，用户下一条常只有「按年」二字（措辞可有变体） */
export const ASSISTANT_ASKED_SUBSCRIPTION_CYCLE_RE =
  /按月还是按年|按年还是按月|按月或按年|月付还是年付|包月还是包年|按月.{0,8}按年|按年.{0,8}按月|续费.{0,6}按月|续费.{0,6}按年/i;
